#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "⏳ Running build before commit..."
npm run build

# Optional: cancel commit if build fails
if [ $? -ne 0 ]; then
  echo "❌ Build failed. Commit aborted."
  exit 1
fi
